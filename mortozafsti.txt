SQL> create table hihi(id number(3) primary key, name varchar2(10));

Table created.

SQL> connect milton
Connected.
SQL> create table hihi(id number(3) primary key, name varchar2(10));

Table created.

SQL> create table hihi(id number(3) primary key, name varchar2(10));
create table hihi(id number(3) primary key, name varchar2(10))
             *
ERROR at line 1:
ORA-00955: name is already used by an existing object 


SQL> spool of
